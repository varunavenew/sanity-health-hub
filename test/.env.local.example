# Copy to .env.local and fill in (never commit .env.local)

# Required — Sanity write token (Editor role)
SANITY_TOKEN=REMOVED_SANITY_TOKEN

# Optional
SANITY_PROJECT_ID=9jhqpk3a
SANITY_DATASET=production

# Optional — higher-quality translation (otherwise free Lingva/MyMemory)
LOVABLE_API_KEY=

# Translation backfill tuning
# TRANSLATE_PROVIDER=lingva
# TRANSLATE_DELAY_MS=1000
# DRY_RUN=1
# FORCE=1
# ONLY=homepage,article
